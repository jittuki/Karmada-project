#appset-propagation.yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: karmada-propagation
  namespace: argocd
spec:
  generators:
    - clusters: {}  # 등록된 모든 클러스터 대상
  template:
    metadata:
      name: '{{cluster.name}}-propagation'
    spec:
      project: default
      source:
        repoURL: 'git@github.com:jittuki/Karmada-project.git'
        targetRevision: main
        path: karmada-propagation/propagation-policies
        directory:
          recurse: true
      destination:
        server: '{{cluster.server}}'
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
