apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: karmada-propagation
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - path: karmada-propagation/workloads
  template:
    metadata:
      name: '{{path}}'
    spec:
      project: default
      source:
        repoURL: 'git@github.com:jittuki/Karmada-project.git'
        targetRevision: main
        path: '{{path}}'
      destination:
        server: 'https://192.168.40.122:32443'  # Karmada API 서버 주소
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
